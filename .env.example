# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ingestion

# Redis
REDIS_URL=redis://localhost:6379

# Authentication
EDGE_API_KEY=your-secret-key-here

# Server
LOG_LEVEL=INFO

# Grafana Cloud (Optional - for LOCAL monitoring with Alloy)
# Only needed if running Grafana Alloy locally via docker-compose
# For Railway: Use Grafana Cloud's "Metrics Endpoint" integration instead
# METRICS_SCRAPE_TARGET=ingestion:8000
